services:
  web:
    build: .
    ports:
      - "80:80"
    deploy:
      resources:
        limits:
          memory: 64M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"